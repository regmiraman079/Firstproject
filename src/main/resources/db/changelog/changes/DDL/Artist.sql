CREATE TABLE artist (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(255),
    href VARCHAR(255),
    spotify_url VARCHAR(255),
    followers INTEGER,
    popularity INTEGER,
    type VARCHAR(100),
    uri VARCHAR(255)
);
